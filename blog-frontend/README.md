## Frontend of Blog Platform

This is the React frontend for the Blog Platform.

### Run
```
npm install
npm start
```

The app expects the backend running at `http://localhost:5000` (configurable in `src/api/axios.js`).
